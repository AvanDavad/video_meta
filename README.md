# video_meta
Get meta info from videos, rename video files etc

Renaming:

```bash
python run_rename_videos.py -i path/to/videos
```

Get duration info:

```bash
python run_rename_videos.py -i path/to/videos
```